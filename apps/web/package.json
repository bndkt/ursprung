{
  "name": "ursprung-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --experimental-new-config",
    "deploy:preview": "wrangler versions upload --experimental-new-config",
    "dev": "wrangler dev --experimental-new-config",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ursprung": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/config": "^0.4.0",
    "wrangler": "^4.119.0"
  }
}
