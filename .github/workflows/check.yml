name: Check

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # The steps themselves live in `checks.yml`, shared with `publish.yml`.
  check:
    uses: ./.github/workflows/checks.yml
